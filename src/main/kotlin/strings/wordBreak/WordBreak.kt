package strings.wordBreak

class WordBreak {
    fun wordBreak(inputString: String, wordDict: List<String>): Boolean {
        val result = StringBuilder()

        for(element in wordDict){

        }
    }

    fun combination (resultToMatch : String, wordDict: List<String>, runningResult : StringBuilder){

    }
}